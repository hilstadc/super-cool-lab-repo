ChatGPT added many features to take safe inputs and improve randomness. To improve input handing, scanf was replaced with fget, and an input buffer was added to prevent buffer overflow. I don't fully understand how this part works. Inputs were also verified to prevent infinite loops, which I do understand. To improve randomness, a seed function was created. This function generates a time-based seed with entropy mixing via bitwise shifts and XOR. A function to generate random numbers within a range was also created so that the loop could be a single printf statement. In the upper bound calculation, a check was added to ensure the upper bound was larger than the lower bound. One downside is ChatGPT tried to move the i declaration inside the for loop, which isn't allowed in our version of C. I had to tell it to fix that. Overall I learned that ChatGPT can be useful for improving code, but can also make mistakes that must be checked.